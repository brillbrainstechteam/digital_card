# Business Card (reserved)

Not implemented yet. Reserved location for the future Business Card product,
which will follow the same pattern as `features/digital-card`: it consumes
`features/qr` for QR generation rather than owning any QR logic itself.
