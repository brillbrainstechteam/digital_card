VITE_CLOUDINARY_CLOUD=dee7qodjv
VITE_CLOUDINARY_PRESET=digital_card_logos

# TEMPORARY — QR add-on payment bypass for local testing before the payment
# gateway exists. Ignored entirely in a production build (see
# src/features/qr/services/qrAccess.js). Set to "false" to see the real
# locked/preview-only flow during dev. Remove this once payments ship.
VITE_ENABLE_QR_DEV_MODE=true
